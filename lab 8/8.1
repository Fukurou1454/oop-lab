#include "pch.h"
#include <iostream>
#include <string>
#include <vector>
#include <Windows.h>
using namespace std;

int main() 
{
	SetConsoleCP(1251);
	SetConsoleOutputCP(1251);

	string name;
	vector<string> students;
	int n;
	cout << "Колливество студентов:";
	cin >> n;
	for (int i = 0; i < n; i++) 
	{
		cout << "Введите имя, фамилию, группу" << endl;
		cin >> name;
		students.push_back(name);
	}
	for (int i = 0; i < n; i++)
	{
		cout << students[i] << endl;
	}

	system("pause");
	return 0;
}
